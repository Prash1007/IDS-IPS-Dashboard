# ─────────────────────────────────────────────
# IDSIPS Dashboard — Python Dependencies
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────

# Core Framework
Django>=6.0,<6.1

# Django REST Framework (API endpoints)
djangorestframework>=3.15,<4.0

# Kafka Consumer (alert ingestion pipeline)
confluent-kafka>=2.3,<3.0

# SSH / Remote Suricata Sync
paramiko>=3.4,<4.0

# Environment variable management (.env support)
python-decouple>=3.8

# Database (PostgreSQL recommended for production)
psycopg2-binary>=2.9,<3.0
# SQLite is used by default in development (no extra package needed)

# WSGI Server (production)
gunicorn>=22.0,<23.0

# Security / CORS headers
django-cors-headers>=4.3,<5.0

# Static files (production)
whitenoise>=6.7,<7.0

# Timezone support
pytz>=2024.1